# JARVIS AI - Render Deployment
# Single Web Service - whole repo deployed as one service

services:
  - type: web
    name: jarvis-ai
    runtime: node
    region: oregon
    buildCommand: cd backend && npm install
    startCommand: cd backend && npm start
    envVars:
      - key: NODE_ENV
        value: production
